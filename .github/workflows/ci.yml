name: CI

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Set up Python
        run: uv python install 3.11

      # Core + dev only — keeps CI fast. The heavy ML extra (torch/lightning/
      # lightgbm) is not installed; model tests skip via `pytest.importorskip`.
      - name: Install (core + dev)
        run: uv sync --extra dev

      - name: Ruff (lint + format check)
        run: |
          uv run ruff check
          uv run ruff format --check

      - name: Mypy
        run: uv run mypy src

      - name: Pytest
        run: uv run pytest
